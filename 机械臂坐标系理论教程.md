# 机械臂坐标系理论教程

## 目录
1. [机械臂坐标系概述](#1-机械臂坐标系概述)
2. [UR5机械臂的主要坐标系](#2-ur5机械臂的主要坐标系)
3. [位姿表示方法](#3-位姿表示方法)
4. [坐标系转换原理](#4-坐标系转换原理)
5. [项目中的实际应用](#5-项目中的实际应用)
6. [常见坐标系转换示例](#6-常见坐标系转换示例)

---

## 1. 机械臂坐标系概述

### 1.1 为什么需要多个坐标系？

机械臂系统涉及多个不同的参考系：
- **基坐标系（Base Frame）**：固定在机械臂底座，是全局参考
- **工具坐标系（Tool/TCP Frame）**：固定在末端执行器，随机械臂运动
- **相机坐标系（Camera Frame）**：固定在相机上，用于视觉定位
- **力传感器坐标系（Force Sensor Frame）**：固定在力传感器上
- **工件坐标系（Workpiece Frame）**：固定在工件上

每个坐标系都有其特定的用途，需要在它们之间进行转换。

### 1.2 坐标系的作用

- **基坐标系**：机械臂所有运动的绝对参考
- **工具坐标系**：描述末端执行器的位置和姿态
- **相机坐标系**：将图像中的像素坐标转换为3D空间坐标
- **力传感器坐标系**：测量力和力矩的方向

---

## 2. UR5机械臂的主要坐标系

### 2.1 基坐标系（Base Frame）

**定义**：固定在机械臂底座，通常用 `{B}` 表示

**特点**：
- 原点在底座中心
- Z轴向上（垂直）
- X轴指向机械臂前方
- Y轴遵循右手定则

**在项目中的使用**：
```python
# 从 robot/Robots/URControlAPI.py
def get_ee_pose(self):
    return self.rtde_r.getActualTCPPose()  # 返回的是基坐标系下的TCP位姿
```

### 2.2 工具坐标系（Tool/TCP Frame）

**定义**：固定在末端执行器（Tool Center Point），用 `{T}` 表示

**特点**：
- 原点在TCP点（通常是工具中心）
- 随机械臂运动而运动
- 用于描述工具的位置和姿态

**TCP位姿表示**：
- 位置：`[x, y, z]` - TCP在基坐标系中的位置
- 姿态：`[rx, ry, rz]` - 旋转向量（rotation vector）或欧拉角

### 2.3 相机坐标系（Camera Frame）

**定义**：固定在相机上，用 `{C}` 表示

**特点**：
- 原点在相机光心
- Z轴沿光轴方向（通常向前）
- X轴向右，Y轴向下（图像坐标系）

**手眼标定**：
项目中通过手眼标定获得相机到工具的变换关系 `cPt`（camera to Tool Pose）

```python
# 从 math/cor_transforme.py 第512行
cPt = [-1.12306, 61.3039, 65.9019, 359.76, 358.368, 179.75]
# 这是相机坐标系到工具坐标系的变换参数（单位：mm和度）
```

### 2.4 力传感器坐标系（Force Sensor Frame）

**定义**：固定在力传感器上，用 `{S}` 表示

**特点**：
- 原点在传感器中心
- 用于测量力和力矩的方向
- 需要转换到基坐标系才能用于控制

---

## 3. 位姿表示方法

### 3.1 位姿的定义

**位姿（Pose）** = **位置（Position）** + **姿态（Orientation）**

- **位置**：3个自由度 `[x, y, z]`
- **姿态**：3个自由度，可以用多种方式表示

### 3.2 姿态的表示方法

#### 3.2.1 旋转向量（Rotation Vector / Rotvec）

**定义**：用一个3维向量表示旋转
- 方向：旋转轴的方向
- 大小：旋转角度（弧度）

**优点**：紧凑（只需3个数），适合UR机械臂

**在项目中的使用**：
```python
# 从 tool/TransPose.py
def getT_fromRotvec(pose):
    x, y, z = pose[0], pose[1], pose[2]
    rx, ry, rz = pose[3], pose[4], pose[5]
    # 从旋转向量创建旋转矩阵
    r = R.from_rotvec(np.array([rx, ry, rz]), False)
    R2 = r.as_matrix()
```

#### 3.2.2 欧拉角（Euler Angles）

**定义**：用三个绕坐标轴的旋转角度表示

**常见形式**：
- **RPY角**：Roll（绕X轴）、Pitch（绕Y轴）、Yaw（绕Z轴）
- **ZYX欧拉角**：先绕Z轴，再绕Y轴，最后绕X轴

**在项目中的使用**：
```python
# 从 math/cor_transforme.py 第358行
def eulerAngles2rotationMat(theta):
    """
    RPY角，是ZYX欧拉角，依次绕定轴XYZ转动[rx, ry, rz]
    """
    R_x = np.array([[1, 0, 0],
                    [0, math.cos(theta[0]), -math.sin(theta[0])],
                    [0, math.sin(theta[0]), math.cos(theta[0])]])
    # ... 类似地定义 R_y 和 R_z
    R = np.dot(R_z, np.dot(R_y, R_x))  # 组合顺序：ZYX
```

#### 3.2.3 旋转矩阵（Rotation Matrix）

**定义**：3×3矩阵，描述坐标系之间的旋转关系

**特点**：
- 正交矩阵：`R^T = R^(-1)`
- 行列式为1：`det(R) = 1`

**在项目中的使用**：
```python
# 从 math/cor_transforme.py 第192行
def MatPose(pose):
    # 从欧拉角创建旋转矩阵
    matRx = MatRx(px)  # 绕X轴旋转
    matRy = MatRy(py)  # 绕Y轴旋转
    matRz = MatRz(pz)  # 绕Z轴旋转
    matR = np.dot(matRz, np.dot(matRy, matRx))  # 组合顺序：ZYX
```

#### 3.2.4 齐次变换矩阵（Homogeneous Transformation Matrix）

**定义**：4×4矩阵，同时表示旋转和平移

**形式**：
```
T = [R  t]    R: 3×3旋转矩阵
    [0  1]    t: 3×1平移向量
```

**优点**：
- 统一表示旋转和平移
- 便于进行坐标系转换

**在项目中的使用**：
```python
# 从 math/cor_transforme.py 第192-218行
def MatPose(pose):
    M = np.zeros((4, 4))
    # 左上角3×3是旋转矩阵
    M[:3, :3] = matR
    # 右上角3×1是平移向量
    M[0][3] = pose[0]  # x
    M[1][3] = pose[1]  # y
    M[2][3] = pose[2]  # z
    # 最后一行是 [0, 0, 0, 1]
    M[3][3] = 1
    return M
```

---

## 4. 坐标系转换原理

### 4.1 齐次变换矩阵

**核心公式**：
```
P_B = T_B^A × P_A
```
其中：
- `P_A`：点P在坐标系A中的坐标
- `P_B`：点P在坐标系B中的坐标
- `T_B^A`：从坐标系A到坐标系B的变换矩阵

### 4.2 变换矩阵的组成

**变换矩阵包含两部分**：
1. **旋转部分**（左上角3×3）：描述坐标系的旋转关系
2. **平移部分**（右上角3×1）：描述坐标系的平移关系

### 4.3 链式变换

**多个坐标系转换**：
```
P_C = T_C^B × T_B^A × P_A
```

**在项目中的应用**：
```python
# 从 math/cor_transforme.py 第498行
# 从相机坐标系到基坐标系的转换链：
# 基坐标系 ← 工具坐标系 ← 相机坐标系 ← 物体在相机坐标系中的位置
P_go = PoseMat_deg(MatPose_deg(wPt) @ MatPose_deg(tPc) @ MatPose_deg(cPo))
#      wPt: 工具在基坐标系中的位姿
#      tPc: 相机在工具坐标系中的位姿（手眼标定参数）
#      cPo: 物体在相机坐标系中的位置
```

### 4.4 逆变换

**求逆变换**：
```
T_A^B = (T_B^A)^(-1)
```

**在项目中的使用**：
```python
# 从 math/cor_transforme.py 第475行
mat_cPt = MatPose_deg(cPt)  # 相机到工具的变换
mat_tPc = np.linalg.pinv(mat_cPt)  # 工具到相机的变换（求逆）
tPc = PoseMat_deg(mat_tPc)
```

---

## 5. 项目中的实际应用

### 5.1 视觉定位流程

**步骤**：
1. **图像处理**：在相机图像中检测物体位置（像素坐标）
2. **像素到相机坐标**：将像素坐标转换为相机坐标系中的3D坐标
3. **相机到工具**：使用手眼标定参数 `cPt`
4. **工具到基坐标**：获取当前TCP位姿 `wPt`
5. **计算目标位置**：在基坐标系中计算物体位置

**代码示例**：
```python
# 从 math/cor_transforme.py 的 cal_world_location 函数
def cal_world_location(match_x, match_y, match_angle, HOST, num=0):
    # 1. 手眼标定参数：相机到工具的变换
    cPt = [-1.12306, 61.3039, 65.9019, 359.76, 358.368, 179.75]
    mat_cPt = MatPose_deg(cPt)
    mat_tPc = np.linalg.pinv(mat_cPt)  # 工具到相机
    tPc = PoseMat_deg(mat_tPc)
    
    # 2. 物体在相机坐标系中的位置
    cPo = [match_x, match_y, 611.82, 0, 0, match_angle]
    
    # 3. 获取当前工具在基坐标系中的位姿
    rob3 = urx.Robot("192.168.1.63")
    wPt = rob3.getl()  # 获取TCP位姿
    for i in range(3):
        wPt[i] *= 1000  # 米转毫米
    
    # 4. 计算物体在基坐标系中的位置
    # P_base = T_base^tool × T_tool^camera × P_camera
    P_go = PoseMat_deg(MatPose_deg(wPt) @ MatPose_deg(tPc) @ MatPose_deg(cPo))
    
    return P_go[0], P_go[1], delta_joint6
```

### 5.2 力传感器坐标转换

**问题**：力传感器测量的力是在传感器坐标系中的，需要转换到基坐标系

**转换公式**：
```
F_base = T_force × F_sensor
```

**代码示例**：
```python
# 从 tool/TransPose.py 第56行
def trans_fromsensor_tobase(T, f_sensor):
    """
    将六维力从传感器坐标转换到基坐标系下表示
    T: 传感器在基坐标系中的变换矩阵
    f_sensor: 传感器坐标系中的力/力矩 [Fx, Fy, Fz, Tx, Ty, Tz]
    """
    Rba = T[:3, :3]  # 旋转矩阵
    x, y, z = T[0, 3], T[1, 3], T[2, 3]  # 平移向量
    
    # 构建6×6的力变换矩阵
    aP_borg = np.mat([
        [0, -z, y],
        [z, 0, -x],
        [-y, x, 0]
    ])
    
    a1 = np.hstack((Rba, np.zeros((3, 3))))
    a2 = np.hstack((aP_borg @ Rba, Rba))
    Tf = np.vstack((a1, a2))
    
    f_base = Tf @ f_sensor
    return f_base
```

**为什么需要6×6矩阵？**
- 力的前3个分量（Fx, Fy, Fz）只需要旋转：`F_base = R × F_sensor`
- 力矩的后3个分量（Tx, Ty, Tz）需要考虑力的作用点：`T_base = R × T_sensor + r × F_base`
- 其中 `r` 是从传感器原点到基坐标系原点的向量

### 5.3 位姿格式转换

**项目中常见的转换**：

1. **旋转向量 ↔ 旋转矩阵**：
```python
# 从 tool/TransPose.py
def getT_fromRotvec(pose):
    r = R.from_rotvec(np.array([rx, ry, rz]), False)
    R2 = r.as_matrix()

def getRotvec_fromT(T):
    r = R.from_matrix(T[0:3, 0:3])
    [rx, ry, rz] = r.as_rotvec()
```

2. **旋转向量 ↔ RPY角**：
```python
# 从 math/cor_transforme.py
def convert_to_rpy(x):
    # 旋转向量 → 旋转矩阵 → RPY角
    (R, R1) = cv2.Rodrigues(aa)  # 旋转向量转旋转矩阵
    # 然后从旋转矩阵提取RPY角

def rpy_to_vec(x):
    # RPY角 → 旋转矩阵 → 旋转向量
    b = eulerAngles2rotationMat(a)
    (c, d) = cv2.Rodrigues(b)
```

---

## 6. 常见坐标系转换示例

### 6.1 示例1：相机坐标系 → 基坐标系

**场景**：相机检测到物体在图像中的位置，需要计算物体在基坐标系中的位置

**转换链**：
```
相机坐标系 → 工具坐标系 → 基坐标系
```

**数学表达**：
```
P_base = T_base^tool × T_tool^camera × P_camera
```

**代码**：
```python
# 1. 手眼标定参数（已知）
cPt = [x, y, z, rx, ry, rz]  # 相机到工具的变换
T_tool^camera = MatPose_deg(cPt)

# 2. 当前工具位姿（从机械臂获取）
wPt = robot.get_ee_pose()  # 工具在基坐标系中的位姿
T_base^tool = MatPose_deg(wPt)

# 3. 物体在相机坐标系中的位置（从图像处理得到）
cPo = [match_x, match_y, depth, 0, 0, angle]

# 4. 计算物体在基坐标系中的位置
P_base = T_base^tool × inv(T_tool^camera) × P_camera
```

### 6.2 示例2：工具坐标系 → 基坐标系

**场景**：在工具坐标系中定义了一个偏移，需要转换到基坐标系

**转换**：
```
P_base = T_base^tool × P_tool
```

**代码**：
```python
# 从 math/tf_transform.py
def calcu_tcp_pose_rotVec(tcp_pose_m3d, tool_delta_pos_m3d):
    """
    计算TCP末端位姿 = 当前TCP位姿 × 工具坐标系中的偏移
    """
    t = tcp_pose_m3d * tool_delta_pos_m3d
    return t.pose_vector
```

### 6.3 示例3：力传感器坐标系 → 基坐标系

**场景**：力传感器测量的力需要转换到基坐标系用于控制

**转换**：
```
F_base = T_force × F_sensor
```

**代码**：
```python
# 从 tool/TransPose.py
def trans_fromsensor_tobase(T, f_sensor):
    """
    T: 传感器在基坐标系中的变换矩阵
    f_sensor: [Fx, Fy, Fz, Tx, Ty, Tz] 在传感器坐标系中
    返回: [Fx, Fy, Fz, Tx, Ty, Tz] 在基坐标系中
    """
    # 构建6×6力变换矩阵
    # ... (见前面代码)
    f_base = Tf @ f_sensor
    return f_base
```

---

## 7. 关键概念总结

### 7.1 坐标系命名约定

- **基坐标系**：`{B}` 或 `{Base}`，固定参考系
- **工具坐标系**：`{T}` 或 `{Tool}`，随机械臂运动
- **相机坐标系**：`{C}` 或 `{Camera}`
- **传感器坐标系**：`{S}` 或 `{Sensor}`

### 7.2 变换矩阵的表示

- `T_B^A`：从坐标系A到坐标系B的变换
- 上标表示目标坐标系，下标表示源坐标系
- 有时也用 `T_BA` 表示

### 7.3 位姿的6个参数

```
pose = [x, y, z, rx, ry, rz]
       └─┬─┘  └────┬────┘
       位置        姿态
```

### 7.4 常见转换公式

1. **点坐标转换**：`P_B = T_B^A × P_A`
2. **链式转换**：`T_C^A = T_C^B × T_B^A`
3. **逆变换**：`T_A^B = (T_B^A)^(-1)`
4. **力/力矩转换**：需要6×6矩阵（考虑力矩的传递）

---

## 8. 实践建议

### 8.1 调试技巧

1. **检查单位**：注意米和毫米的转换
2. **检查角度单位**：注意弧度和度的转换
3. **验证逆变换**：`T × T^(-1)` 应该等于单位矩阵
4. **可视化**：在机械臂上标记关键点，验证转换是否正确

### 8.2 常见错误

1. **旋转顺序错误**：ZYX和XYZ顺序不同
2. **单位不一致**：位置用米，角度用度
3. **坐标系方向**：注意右手坐标系和左手坐标系
4. **手眼标定参数错误**：导致视觉定位不准确

### 8.3 学习资源

1. **《机器人学导论》**：John J. Craig 著
2. **UR官方文档**：了解UR机械臂的坐标系定义
3. **OpenCV文档**：了解旋转向量和旋转矩阵的转换
4. **项目代码**：仔细阅读 `math/cor_transforme.py` 和 `tool/TransPose.py`

---

## 9. 练习题

### 练习1：理解变换链

在 `cal_world_location` 函数中，为什么使用：
```python
P_go = MatPose_deg(wPt) @ MatPose_deg(tPc) @ MatPose_deg(cPo)
```
而不是：
```python
P_go = MatPose_deg(wPt) @ MatPose_deg(cPt) @ MatPose_deg(cPo)
```

**答案**：因为 `tPc` 是工具到相机的变换（`cPt` 的逆），而我们需要的是从相机坐标系到工具坐标系，再到基坐标系的转换链。

### 练习2：理解力转换

为什么力传感器坐标转换需要6×6矩阵，而不是简单的3×3旋转矩阵？

**答案**：因为力矩的转换需要考虑力的作用点。如果力的作用点不在坐标系原点，会产生额外的力矩。

---

## 10. 总结

机械臂坐标系转换是机器人控制的基础，掌握以下要点：

1. **理解各个坐标系的作用**：基坐标、工具坐标、相机坐标等
2. **掌握位姿表示方法**：旋转向量、欧拉角、旋转矩阵、齐次变换矩阵
3. **理解变换链**：多个坐标系之间的链式转换
4. **实践应用**：视觉定位、力控制等实际场景

通过本教程和项目代码的学习，你应该能够：
- 理解项目中各个坐标系转换的代码
- 根据需求编写新的坐标系转换函数
- 调试和解决坐标系转换相关的问题

祝你学习顺利！
