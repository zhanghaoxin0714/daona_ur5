# 机械臂坐标系转换快速参考

## 📚 坐标系速查表

| 坐标系 | 符号 | 定义 | 用途 |
|--------|------|------|------|
| 基坐标系 | {B} | 固定在机械臂底座 | 全局参考，所有运动的绝对坐标 |
| 工具坐标系 | {T} | 固定在TCP（工具中心点） | 描述末端执行器的位置和姿态 |
| 相机坐标系 | {C} | 固定在相机 | 将图像坐标转换为3D空间坐标 |
| 力传感器坐标系 | {S} | 固定在力传感器 | 测量力和力矩的方向 |

---

## 🔄 位姿表示方法

### 1. 旋转向量（Rotvec）- UR机械臂常用
```python
pose = [x, y, z, rx, ry, rz]
#      位置(米)  旋转向量(弧度)
```

### 2. RPY角（Roll-Pitch-Yaw）
```python
rpy = [roll, pitch, yaw]  # 弧度
# 顺序：ZYX（先绕Z轴，再Y轴，最后X轴）
```

### 3. 旋转矩阵（3×3）
```python
R = [[r11, r12, r13],
     [r21, r22, r23],
     [r31, r32, r33]]
```

### 4. 齐次变换矩阵（4×4）
```python
T = [[R11, R12, R13, tx],
     [R21, R22, R23, ty],
     [R31, R32, R33, tz],
     [ 0,   0,   0,   1]]
# 左上角3×3：旋转矩阵
# 右上角3×1：平移向量
```

---

## 🧮 核心转换公式

### 点坐标转换
```
P_B = T_B^A × P_A
```
- `P_A`: 点在坐标系A中的坐标
- `P_B`: 点在坐标系B中的坐标
- `T_B^A`: 从A到B的变换矩阵

### 链式转换
```
T_C^A = T_C^B × T_B^A
```
多个坐标系依次转换

### 逆变换
```
T_A^B = (T_B^A)^(-1)
```
求逆得到反向变换

---

## 💻 项目中的关键函数

### 1. 位姿 ↔ 变换矩阵
```python
# math/cor_transforme.py
def MatPose(pose):  # 位姿 → 变换矩阵
def PoseMat(M):     # 变换矩阵 → 位姿
```

### 2. 旋转向量 ↔ 旋转矩阵
```python
# tool/TransPose.py
def getT_fromRotvec(pose):      # 旋转向量 → 变换矩阵
def getRotvec_fromT(T):         # 变换矩阵 → 旋转向量
```

### 3. 视觉定位转换链
```python
# math/cor_transforme.py - cal_world_location()
P_base = T_base^tool × T_tool^camera × P_camera
# 其中：
# - T_base^tool: 从机械臂获取的当前TCP位姿
# - T_tool^camera: 手眼标定参数（cPt的逆）
# - P_camera: 从图像处理得到的物体位置
```

### 4. 力传感器转换
```python
# tool/TransPose.py
def trans_fromsensor_tobase(T, f_sensor):
    # 将6维力从传感器坐标系转换到基坐标系
    # 需要6×6矩阵（考虑力矩传递）
```

---

## 🔍 常见转换场景

### 场景1：视觉定位
```
图像像素 → 相机坐标 → 工具坐标 → 基坐标
```
**代码位置**: `math/cor_transforme.py` 的 `cal_world_location()`

### 场景2：力控制
```
传感器力 → 基坐标力
```
**代码位置**: `tool/TransPose.py` 的 `trans_fromsensor_tobase()`

### 场景3：相对运动
```
工具坐标系中的偏移 → 基坐标系中的偏移
```
**代码位置**: `math/tf_transform.py` 的 `calcu_tcp_pose_rotVec()`

---

## ⚠️ 常见错误和注意事项

### 1. 单位问题
- ✅ UR机械臂API返回的位置单位是**米**
- ✅ 项目代码中很多地方转换为**毫米**（×1000）
- ✅ 角度单位：注意**弧度**和**度**的转换

### 2. 旋转顺序
- ✅ RPY角使用**ZYX顺序**（先Z，再Y，最后X）
- ✅ 代码中：`R = Rz @ Ry @ Rx`

### 3. 手眼标定参数
- ✅ `cPt`: 相机到工具的变换（camera to Tool Pose）
- ✅ `tPc`: 工具到相机的变换（tool to Camera Pose）= `cPt`的逆

### 4. 变换矩阵的方向
- ✅ `T_B^A`: 从A到B的变换
- ✅ 上标是目标，下标是源

---

## 📖 学习路径建议

1. **理解基础概念**
   - 阅读 `机械臂坐标系理论教程.md`
   - 理解各个坐标系的作用

2. **查看实际代码**
   - `math/cor_transforme.py`: 视觉定位相关
   - `tool/TransPose.py`: 通用坐标转换
   - `math/tf_transform.py`: 工具坐标系转换

3. **运行示例代码**
   - 运行 `坐标系转换示例代码.py`
   - 理解每个示例的含义

4. **实践调试**
   - 在项目中添加打印语句，查看中间结果
   - 验证转换是否正确

---

## 🔗 相关文件索引

| 文件 | 主要内容 |
|------|----------|
| `机械臂坐标系理论教程.md` | 详细的理论教程 |
| `坐标系转换示例代码.py` | 可运行的示例代码 |
| `math/cor_transforme.py` | 视觉定位的坐标转换 |
| `tool/TransPose.py` | 通用坐标转换工具类 |
| `math/tf_transform.py` | 工具坐标系相关转换 |

---

## 💡 快速调试技巧

### 检查变换矩阵是否正确
```python
T = MatPose(pose)
T_inv = np.linalg.pinv(T)
T_check = T @ T_inv  # 应该接近单位矩阵
print(T_check)  # 应该看到对角线为1，其他接近0
```

### 验证点转换
```python
# 在基坐标系中的点
point_base = [0.3, 0.2, 0.5]
# 转换到工具坐标系
T_base_to_tool = MatPose(tool_pose)
T_tool_to_base = np.linalg.pinv(T_base_to_tool)
point_tool = transform_point(T_tool_to_base, point_base)
# 再转换回来
point_base_check = transform_point(T_base_to_tool, point_tool)
# 应该和原始点相同
```

### 打印中间结果
```python
print(f"工具位姿: {tool_pose}")
print(f"相机到工具: {cPt}")
print(f"物体在相机中: {cPo}")
print(f"最终位置: {P_go}")
```

---

## 📝 记忆口诀

- **位姿6参数**：位置3个 + 姿态3个
- **变换4×4矩阵**：旋转3×3 + 平移3×1
- **链式转换**：从左到右依次相乘
- **逆变换**：矩阵求逆或手动计算
- **单位注意**：米/毫米，弧度/度

---

**最后更新**: 请结合项目实际代码理解这些概念！
